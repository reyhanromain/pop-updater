# Telegram credentials for pop-updater notifications
# Copy this file to /etc/pop-updater/secrets.conf and fill in the values.
# Never commit the actual secrets.conf — it lives only on the machine.

TG_BOT_TOKEN=your_bot_token_here
TG_CHAT_ID=your_chat_id_here
